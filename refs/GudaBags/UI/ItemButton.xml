<Ui xmlns="http://www.blizzard.com/wow/ui/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.blizzard.com/wow/ui/">

    <!-- Item Button Template (inherit Blizzard container item button for classic behavior/skins) -->
    <Button name="Guda_ItemButtonTemplate" virtual="true" inherits="ContainerFrameItemButtonTemplate">
        <Size>
            <AbsDimension x="37" y="37"/>
        </Size>

        <Layers>
            <!-- Quality color ring (slightly larger UI-EmptySlot behind the normal one) -->
            <Layer level="BACKGROUND" textureSubLevel="-1">
                <Texture name="$parent_QualityRing" file="Interface\Buttons\UI-EmptySlot" hidden="true">
                    <Anchors>
                        <Anchor point="TOPLEFT"/>
                        <Anchor point="BOTTOMRIGHT"/>
                    </Anchors>
                </Texture>
            </Layer>
            <!-- Empty slot background texture (classic bag pattern, covers center of quality ring) -->
            <Layer level="BACKGROUND">
                <Texture name="$parent_EmptySlotBg" file="Interface\Buttons\UI-EmptySlot" hidden="false">
                    <Anchors>
                        <Anchor point="TOPLEFT"/>
                        <Anchor point="BOTTOMRIGHT"/>
                    </Anchors>
                </Texture>
            </Layer>
            
            <!-- Custom item count (this intentionally co-exists with Blizzard's $parentCount in rollback state) -->
            <Layer level="OVERLAY">
                <FontString name="$parent_Count" inherits="NumberFontNormal" justifyH="RIGHT">
                    <Anchors>
                        <Anchor point="BOTTOMRIGHT">
                            <Offset>
                                <AbsDimension x="-8" y="8"/>
                            </Offset>
                        </Anchor>
                    </Anchors>
                </FontString>
                <FontString name="$parent_Charges" inherits="NumberFontNormal" justifyH="RIGHT" hidden="true">
                    <Color r="1" g="0.82" b="0"/>
                    <Anchors>
                        <Anchor point="BOTTOMRIGHT">
                            <Offset>
                                <AbsDimension x="-8" y="20"/>
                            </Offset>
                        </Anchor>
                    </Anchors>
                </FontString>
                <Texture name="$parent_Check" file="Interface\Buttons\UI-CheckBox-Check" hidden="true">
                    <Size>
                        <AbsDimension x="24" y="24"/>
                    </Size>
                    <Anchors>
                        <Anchor point="CENTER"/>
                    </Anchors>
                </Texture>
            </Layer>
        </Layers>

        <Scripts>
            <OnLoad>
                -- Our visual setup
                Guda_ItemButton_OnLoad(this)
                -- Ensure Blizzard default container item button behavior is active.
                -- Ascension Epoch's ContainerFrame.lua uses `self` as a parameter
                -- (not the vanilla `this` implicit global), so pass it explicitly.
                if ContainerFrameItemButton_OnLoad then
                    ContainerFrameItemButton_OnLoad(this)
                end
                -- Neutralize Blizzard's tooltip auto-refresh on this client.
                -- ContainerFrameItemButton_OnUpdate keeps re-firing
                -- ContainerFrameItemButton_OnEnter every TOOLTIP_UPDATE_TIME,
                -- which re-anchors the tooltip to ANCHOR_LEFT/RIGHT
                -- (visible "tooltip jumps -5/-10px" effect on bags) and
                -- calls SetBagItem; for bank slots that call returns false
                -- while Blizzard's BankFrame is hidden (we hide it via
                -- HideBlizzardBank), and the OnEnter then GameTooltip:Hide()s
                -- the tooltip we just populated.
                --
                -- Three guards (any one would suffice; we apply all three
                -- because Ascension Epoch's modified ContainerFrame.lua may
                -- have stripped the standard `if not updateTooltip` check
                -- and another addon may re-attach OnUpdate behind us):
                --   1. SetScript OnUpdate nil  — kills the poll script
                --   2. updateTooltip = nil     — disables the standard guard
                --   3. UpdateTooltip = noop    — neuters direct method calls
                this:SetScript("OnUpdate", nil)
                this.updateTooltip = nil
                this.UpdateTooltip = function() end
            </OnLoad>
            <OnEnter>
                Guda_ItemButton_OnEnter(this)
            </OnEnter>
            <OnLeave>
                Guda_ItemButton_OnLeave(this)
            </OnLeave>
        </Scripts>
    </Button>

</Ui>
