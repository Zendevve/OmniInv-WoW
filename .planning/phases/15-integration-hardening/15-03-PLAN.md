---
wave: 2
depends_on:
  - 15-01-PLAN.md
files_modified:
  - Omni/Data.lua
  - docs/Testing/integration-test-matrix.md
requirements_addressed:
  - "(Cross-cutting stability)"
autonomous: true
---

# Plan 03: SavedVariables Audit and Verification Documentation

## Objective
Validate that all v2.3 SavedVariables keys stay within limits and produce no errors when missing. Document the manual verification test matrix covering all four features simultaneously.

## Background
Phases 11-14 introduced these net-new saved keys:
- `OmniInventoryDB.global.enableVirtualStacks` (bool, Phase 11)
- `OmniInventoryDB.char.virtualStackOverrides` (table, Phase 11)
- `OmniInventoryDB.global.enableEmptySlotCompression` (bool, Phase 12)
- `OmniInventoryDB.char.blizzardSetCache` (table, Phase 14)

The defaults table in `Data.lua:16-57` should already include these keys. The audit verifies they exist in defaults and have proper nil-guards everywhere they're read.

## Tasks

### Task 3.1: Audit SavedVariables defaults and nil-guards

<read_first>
- Omni/Data.lua (lines 16-57 — defaults table)
- Omni/Data.lua (lines 85-117 — Init function, MergeDefaults)
- Omni/Data.lua (lines 337-430 — Phase 11-14 data functions)
- UI/Frame.lua (lines 1504,1557 — where enableVirtualStacks and enableEmptySlotCompression are read)
- UI/Options.lua (lines around 150-180 — where toggles are written)
</read_first>

<action>
1. Verify that `defaults.global` contains all v2.3 keys with correct default values:

```lua
enableVirtualStacks = true,
enableEmptySlotCompression = true,
```

These are already at `Data.lua:24-25`. Confirm they exist with these exact values.

2. Verify that `defaults.char` contains `virtualStackOverrides`:

```lua
virtualStackOverrides = {},  -- { [itemID] = true }
```

This is already at `Data.lua:54`. Confirm it exists.

3. `blizzardSetCache` is deliberately NOT in the defaults table (it's rebuilt at runtime). Verify that all reads check for nil:

- `Data:GetBlizzardSetsForItem()` at line 402-408 checks `self.blizzardItemToSets` (already has nil guard from Task 2.4)
- `Data:GetAllGearSetNames()` at line 417-435 checks `OmniInventoryDB.char.blizzardSetCache` (already has nil guard at line 428-433)

4. Add nil-guard for `enableVirtualStacks` at `Frame.lua:1504` if not already present. Same pattern as `enableEmptySlotCompression`:

```lua
if Omni.VirtualStacks and OmniInventoryDB and OmniInventoryDB.global and OmniInventoryDB.global.enableVirtualStacks ~= false then
```

5. Verify nil-guard for `enableEmptySlotCompression` at `Frame.lua:1557` — this already has the `~= false` pattern which is correct.

6. Run a mental audit: for any key that could be nil (first login, fresh install), the code path must handle nil gracefully. Keys in defaults table are covered by `MergeDefaults()`. Keys NOT in defaults (like `blizzardSetCache`) need explicit nil guards — verify these exist.
</action>

<acceptance_criteria>
- `Omni/Data.lua` line 24 contains `enableVirtualStacks = true`
- `Omni/Data.lua` line 25 contains `enableEmptySlotCompression = true`
- `Omni/Data.lua` line 54 contains `virtualStackOverrides = {}`
- `UI/Frame.lua` line 1504 contains `~= false` guard for `enableVirtualStacks`
- `Omni/Data.lua` `GetBlizzardSetsForItem()` contains `if not self.blizzardItemToSets then return {} end`
- `Omni/Data.lua` `GetAllGearSetNames()` contains `if OmniInventoryDB.char.blizzardSetCache then`
</acceptance_criteria>

### Task 3.2: Create integration test matrix document

<read_first>
- .planning/phases/15-integration-hardening/15-CONTEXT.md (D-12 verification matrix)
- .planning/ROADMAP.md (Phase 15 success criteria)
</read_first>

<action>
Create `docs/Testing/integration-test-matrix.md` with the following documented test scenarios:

```markdown
# v2.3 Integration Test Matrix

> Manual in-game verification for all v2.3 features working together.

## Setup
- All four features enabled: Virtual Stacks, Empty Slot Compression, Context Menu, Gear Set Integration
- Bags contain a mix of: equipment (some in sets), consumables, quest items, trade goods, junk
- At least one equipment set configured in Blizzard Equipment Manager

## Test Scenarios

### 1. All Features Enabled — No Errors
- Open bags in Grid view → no Lua errors
- Switch to Flow view → no Lua errors, categories render
- Switch to List view → no Lua errors, list renders
- Open bank → no Lua errors
- Close bags → no Lua errors

### 2. Virtual Stacks + Empty Compression
- Enable both: no blank spaces between categories
- "Empty (N)" count matches visible empty space
- Toggle empty compression off → individual empty slots appear
- Toggle empty compression on → single compressed section

### 3. Context Menu + Virtual Stack Items
- Right-click a virtual stack (item with multiple sources)
- Click "Use Item" → consumes from correct source slot
- Right-click again → menu still works (doesn't use stale slot)

### 4. Gear Set Filter + Empty Compression
- Right-click Gear button → select a set
- Empty slot count updates to reflect filtered view
- Select "All Equipment" → empty count returns to normal

### 5. Toggle Features Rapidly
- Toggle virtual stacks on/off 5 times rapidly
- Toggle empty compression on/off 5 times rapidly
- Open/close bags between toggles
- No state corruption, no orphaned buttons

### 6. Extended Play Session (30+ min)
- Open bags, leave open for 30 minutes
- Observe memory via /dump GetAddOnMemoryUsage("OmniInventory")
- Memory should not continuously grow
- No FPS drops during bag open/close

### 7. Bank with All Features
- Open bank with all features enabled
- Categories render correctly
- Virtual stacks work with bank items
- Gear sets show for bank items

## Notes
- Test each scenario in all 3 view modes (Grid, Flow, List)
- If any error occurs, note the exact error message and steps to reproduce
- Expected: zero Lua errors, zero FPS drops, stable memory
```

The document should be placed at `docs/Testing/integration-test-matrix.md`.
</action>

<acceptance_criteria>
- `docs/Testing/integration-test-matrix.md` exists
- Contains exactly 7 test scenarios with numbered headings
- Each scenario has clear expected behavior
- Document mentions all 3 view modes (Grid, Flow, List) for testing
- Document notes to check memory and FPS in scenario 6
</acceptance_criteria>

## Verification Criteria
- [ ] All v2.3 SavedVariables keys have proper defaults or nil-guards
- [ ] Fresh install (no OmniInventoryDB) won't produce errors
- [ ] Integration test matrix document exists with 7 scenarios
- [ ] Test matrix covers all 6 roadmap success criteria
